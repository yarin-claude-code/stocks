---
phase: 03-api-dashboard
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05]

must_haves:
  truths:
    - "Dashboard renders top 5 stocks per selected domain"
    - "Clicking a stock shows score breakdown modal"
    - "Skeleton states visible on initial load"
    - "Market Closed banner appears when market is closed"
    - "Layout is responsive on mobile and desktop"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the full dashboard UI in a real browser.

Purpose: Confirm all visual and interactive requirements work end-to-end before phase is considered complete.
Output: Approved UI or list of issues for fix.
</objective>

<execution_context>
@C:/Users/Yarin David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Yarin David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start backend + frontend</name>
  <files></files>
  <action>
Start both servers:
```bash
# Terminal 1
cd backend && uvicorn app.main:app --reload

# Terminal 2
cd frontend && npm run dev
```
Wait for backend startup log confirming scheduler started. Wait for Vite dev server ready message.
  </action>
  <verify>
```bash
curl http://localhost:8000/api/health
curl http://localhost:8000/api/domains
```
Both return 200 JSON.
  </verify>
  <done>Both servers running. API returning data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dashboard in browser</name>
  <files></files>
  <action>
Pause for human to verify the dashboard. See how-to-verify checklist below.
  </action>
  <verify>User confirms all checklist items pass and types "approved".</verify>
  <done>User has approved the dashboard UI in browser.</done>
  <what-built>
Full dashboard: domain tabs, top-5 stock cards per domain, BestOverall card, ScoreBreakdown modal, skeleton loading states, Market Closed banner, responsive layout.
  </what-built>
  <how-to-verify>
1. Visit http://localhost:5173
2. Confirm skeleton placeholders show briefly on load (or are visible on slow network throttle in DevTools)
3. Confirm domain tabs appear (should see 12 domains: AI/Tech, EV, Finance, etc.)
4. Click a domain tab — top 5 stocks for that domain appear
5. Confirm BestOverall card shows the highest-scored stock
6. Click a stock card — ScoreBreakdown modal opens with factor values
7. Click outside modal or X button — modal closes
8. Resize browser to mobile width (< 768px) — cards stack to single column
9. Check Market Closed banner: if outside NYSE hours (9:30am–4pm ET Mon–Fri), amber banner shows in header

If ranking_results table is empty (no fetch_cycle has run), trigger a manual fetch by waiting ~5 minutes or restarting the backend (scheduler fires on start). Alternatively check logs for "persisted N ranking results".
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All items in how-to-verify checklist pass.
</verification>

<success_criteria>
User types "approved" — all UI requirements verified in browser.
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-dashboard/03-04-SUMMARY.md`
</output>
